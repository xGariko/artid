# Base URL of the Spring backend (server-side only).
API_BASE=http://localhost:8080

# Auth cookie lifetime in seconds. Keep aligned with `jwt.expiration-ms` on
# the backend (Spring default: 86400000 ms = 86400 s = 24h).
AUTH_COOKIE_MAX_AGE_SECONDS=86400
